/// <reference types="Cypress" />

describe('Limitations of Frames with Cypress', function()
{
    it('Frames test case', function()
    {
/*
Currently the driver does not support selecting or accessing elements from within an iframe.
 This isn't a technical limitation - its an oversight in the driver.
*/        
    })
})